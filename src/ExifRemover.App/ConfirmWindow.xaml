<Window x:Class="ExifRemover.App.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm metadata removal"
        Width="560" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource HeaderText}" Text="Remove metadata from these files?" />
        <TextBlock Grid.Row="1" Style="{StaticResource SubtleText}" Margin="0,4,0,12"
                   x:Name="ProfileSummaryText" />

        <Border Grid.Row="2" BorderBrush="#D8D8D8" BorderThickness="1" Background="White">
            <ListBox x:Name="FileList" BorderThickness="0" FontFamily="{StaticResource MonoFontFamily}"
                     FontSize="{StaticResource UiFontSizeSmall}" />
        </Border>

        <CheckBox Grid.Row="3" Margin="0,12,0,0" x:Name="DontAskAgainCheck"
                  Content="Don't ask again for this session" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="No" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    IsCancel="True" Click="No_Click" />
            <Button Content="Yes, remove" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="Yes_Click" />
        </StackPanel>
    </Grid>
</Window>